post_install() {
  echo ">>> Msty Claw installed successfully."
  echo ">>> Launch with: msty-claw"
  update-desktop-database -q
  if command -v gtk-update-icon-cache >/dev/null 2>&1; then
    gtk-update-icon-cache -q -t -f /usr/share/icons/hicolor || true
  fi
}

post_upgrade() {
  post_install
}
